// @flow

import React from 'react';

type Props = {
  html: string,
};

/**
* a component to make using dangerouslySetInnerHTML easier.
*/
const HtmlFragment = (props: Props) => {};


export default HtmlFragment;
