// @flow

import {
  getActiveClass,
  getActivePupil,
  getActiveText,
  getActiveReport,
  getActiveCategory,
} from '../redux';


/**
* Redux Tests
*/

describe('Util: Redux', () => {
  test('getActiveClass() returns the matching class', () => {
  });
});
